<%= form_for(@event_comment) do |f| %>
  <% if @event_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_comment.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment_type %><br>
    <%= f.text_field :comment_type %>
  </div>
  <div class="field">
    <%= f.label :comment_title %><br>
    <%= f.text_field :comment_title %>
  </div>
  <div class="field">
    <%= f.label :comment_body %><br>
    <%= f.text_field :comment_body %>
  </div>
  <div class="field">
    <%= f.label :comment_url %><br>
    <%= f.text_field :comment_url %>
  </div>
  <%= f.hidden_field :user_id, :value => current_user.id %>
  <%= f.hidden_field :event_id, :value => @event.id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
