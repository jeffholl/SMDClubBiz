<h1>Editing a users membership</h1>
<p><strong>Members Name</strong><br><%= @membership.user.displayname %></p>
<p><strong>Club</strong><br><%= @membership.club.club_name %></p>

<%= form_for(@membership) do |f| %>
  <% if @membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@membership.errors.count, "error") %> prohibited this membership from being saved:</h2>

      <ul>
      <% @membership.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div>
	<div class="actions">
    <%= f.submit %>
  </div>
<% end %>