<%= form_for(@ticket_allocation) do |f| %>
  <% if @ticket_allocation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_allocation.errors.count, "error") %> prohibited this ticket allocation from being saved:</h2>

      <ul>
      <% @ticket_allocation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.collection_select(:user_id, User.all, :id, :first_name, prompt: true) %>
  </div>
  <div class="field">
    <%= f.label :ticket_id %><br>
    <%= f.collection_select(:ticket_id, Ticket.all, :id, :ticket_type, prompt: true) %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br>
    <%= f.text_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>