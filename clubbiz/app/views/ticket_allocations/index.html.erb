<h1>Listing all ticket allocations</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>ticket_id</th>
      <th>user_id</th>
      <th>quantity</th>
    </tr>
  </thead>

  <tbody>
    <% @ticket_allocations.each do |ticket_allocation| %>
      <tr>
        <td><%= ticket_allocation.ticket.ticket_name %></td>
        <td><%= ticket_allocation.user.first_name %></td>
        <td><%= ticket_allocation.quantity %></td>
      </tr>
    <% end %>
  </tbody>
</table>